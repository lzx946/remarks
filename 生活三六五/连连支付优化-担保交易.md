

```sql

ALTER TABLE `96365_pay`.`payee_info` ADD COLUMN wx_fee_flag VARCHAR(1) DEFAULT '0' COMMENT '微信手续费标志，0=非微信手续费；1=微信手续费。' AFTER `peyeeinfo`;

ALTER TABLE pay_detail ADD COLUMN wx_fee int(11) NOT NULL DEFAULT 0 COMMENT '微信手续费' AFTER `channel_app_id`;
ALTER TABLE pay_detail ADD COLUMN ll_fee int(11) NOT NULL DEFAULT 0 COMMENT '连连手续费' AFTER `wx_fee`;

```