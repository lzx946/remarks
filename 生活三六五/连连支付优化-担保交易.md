

```sql

ALTER TABLE `96365_pay`.`payee_info` ADD COLUMN wx_fee_flag VARCHAR(1) DEFAULT '0' COMMENT '微信手续费标志，0=非微信手续费；1=微信手续费。' AFTER `peyeeinfo`;

ALTER TABLE pay_detail ADD COLUMN wx_fee int(11) NOT NULL DEFAULT 0 COMMENT '微信手续费' AFTER `channel_app_id`;
ALTER TABLE pay_detail ADD COLUMN ll_fee int(11) NOT NULL DEFAULT 0 COMMENT '连连手续费' AFTER `wx_fee`;


CREATE TABLE `error_log` (
  `error_id` VARCHAR(64) NOT NULL COMMENT '错误日志ID',
  `error_type` VARCHAR(64) NOT NULL COMMENT '错误类型',
  `ip` varchar(100) NOT NULL COMMENT '服务端ip',
  `url` varchar(255) DEFAULT NULL COMMENT '访问URL',
  `params` varchar(1000) DEFAULT NULL COMMENT '方法参数',
  `message` VARCHAR(255) DEFAULT NULL COMMENT '错误日志',
  `stack_trace` text DEFAULT NULL COMMENT '错误堆栈信息',
  `create_date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '记录时间',
  PRIMARY KEY (`error_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='错误日志表';

```