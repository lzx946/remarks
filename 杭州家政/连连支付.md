```json
{
	"flag_chnl": "H5",
	"user_type": "INNERCOMPANY",
	"basicInfo": {											//开户基本信息
		"reg_phone": "137xxxxxxxx",								//用户开户绑定手机号
		"user_name": "xxxx 有限公司",							//企业名称
		"id_no": "91430111MA4L16JQ9B",							//社会统一信用代码
		"address": "xxx 省 xx 市 xx 区 xx 街道 xx 号",			//企业地址
		"reg_email": "xxxx@126.com",							//注册邮箱
		"password": "",											//密码
		"random_key": ""										//随机因子
	},
	"linkedAcctInfo": {										//开户绑卡信息
		"linked_acctname": "",									//对公账户名
		"linked_acctno": "19-4661219xxxxxxxxx", 				//企业用户绑定的银行帐号
		"linked_bankcode": "01030000",							//银行编码
		"linked_phone": "",										//银行预留手机号,可不填
		"linked_brbankname": "",								//企业用户绑定账户开户支行名称
		"linked_brbankno": "010300001234"						//企业用户开户行行号，企业用户必填
	},
	"legalreptInfo": {										//企业法定代表人信息
		"legalrept_idno": ""									//法定代表人身份证号
		"legalrept_name": "张三",								//法定代表人姓名
		"legalrept_phone": "137xxxxxxxx"						//法定代表人手机号
	},
	"basicAcctInfo": {										//企业基本户信息
		"basicacct_bankcode": "01030000",						//企业基本户银行编码
		"basicacct_no": "19-2903020xxxxxxx"						//企业基本户帐号
	},
	"contactsInfo": {										//企业联系人信息
		"contacts_name": "",									//联系人姓名
		"contacts_phone": ""									//联系人手机号
	}
}
```


### 待同步数据库sql
```sql
ALTER TABLE `96365_order`.order_payment ADD COLUMN accp_txno VARCHAR(25) DEFAULT NULL COMMENT '连连支付ACCP系统交易单号';

CREATE TABLE `96365_order`.`lianlian_refund` (
  `refund_id` varchar(20) NOT NULL COMMENT '退款ID',
  `object_id` varchar(20) NOT NULL COMMENT '退款关联单号ID',
	`object_type` varchar(20) NOT NULL COMMENT '退款类型',
	`refund_amount` int(10) NOT NULL COMMENT '退款金额',
	`accp_txno` varchar(30) NOT NULL COMMENT 'ACCP系统退款单号',
	`refund_status` varchar(20) NOT NULL COMMENT '退款状态',
	`accounting_date` varchar(20) DEFAULT NULL COMMENT '账务日期',
	`chnl_txno` varchar(30) DEFAULT NULL COMMENT '渠道退款单号',
  `del_flag` varchar(1) DEFAULT '1' COMMENT '逻辑删除 （1 正常 0删除）',
	`create_date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
	`create_id` varchar(20) DEFAULT NULL COMMENT '创建人',
  `modify_date` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  `modify_id` varchar(20) DEFAULT NULL COMMENT '修改人',
  PRIMARY KEY (`refund_id`),
  UNIQUE KEY `accp_txno_index` (`accp_txno`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='连连支付退款记录';

CREATE TABLE `96365_order`.`transfer` (
  `transfer_id` varchar(20) NOT NULL COMMENT '分账ID',
	`order_id` varchar(20) NOT NULL COMMENT '订单ID',
  `merchant_id` varchar(20) NOT NULL COMMENT '商户ID',
	`employee_id` varchar(20) NOT NULL COMMENT '服务人员ID',
	`total_amount` int(10) NOT NULL COMMENT '分账总金额',
	`mer_txn_seqno` varchar(20) DEFAULT NULL COMMENT '商户分账交易单号',
	`mer_accp_txno` varchar(30) DEFAULT NULL COMMENT 'ACCP系统交易单号(商户)',
	`mer_transfer_amount` int(10) DEFAULT NULL COMMENT '商户分账金额',
	`mer_transfer_status` varchar(20) DEFAULT NULL COMMENT '商户分账状态',
	`emp_txn_seqno` varchar(20) DEFAULT NULL COMMENT '服务人员分账交易单号',
	`emp_accp_txno` varchar(30) DEFAULT NULL COMMENT 'ACCP系统交易单号(服务人员)',
	`emp_transfer_amount` int(10) DEFAULT NULL COMMENT '服务人员分账金额',
	`emp_transfer_status` varchar(20) DEFAULT NULL COMMENT '服务人员分账状态',
  `del_flag` varchar(1) DEFAULT '1' COMMENT '逻辑删除 （1 正常 0删除）',
	`create_date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
	`create_id` varchar(20) DEFAULT NULL COMMENT '创建人',
  `modify_date` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  `modify_id` varchar(20) DEFAULT NULL COMMENT '修改人',
  PRIMARY KEY (`transfer_id`),
	UNIQUE KEY `order_id_index` (`order_id`),
  UNIQUE KEY `accp_txno_txn_seqno_index_mer` (`mer_accp_txno`,`mer_txn_seqno`),
	UNIQUE KEY `accp_txno_txn_seqno_index_emp` (`emp_accp_txno`,`emp_txn_seqno`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='分账记录';


CREATE TABLE `96365_order`.`withdrawal` (
  `withdrawal_id` varchar(20) NOT NULL COMMENT '提现ID',
  `payer_id` varchar(20) NOT NULL COMMENT '提现方ID(付款方ID)',
	`payer_type` varchar(20) NOT NULL COMMENT '提现方类型(付款方类型)',
	`total_amount` int(10) NOT NULL COMMENT '提现金额',
	`postscript` varchar(255) DEFAULT NULL COMMENT '交易附言',
	`accp_txno` varchar(30) DEFAULT NULL COMMENT 'ACCP系统交易单号',
	`status` varchar(20) NOT NULL COMMENT '提现状态',
	`failure_reason` varchar(255) DEFAULT NULL COMMENT '提现失败原因',
  `del_flag` varchar(1) DEFAULT '1' COMMENT '逻辑删除 （1 正常 0删除）',
	`create_date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
	`create_id` varchar(20) DEFAULT NULL COMMENT '创建人',
  `modify_date` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  `modify_id` varchar(20) DEFAULT NULL COMMENT '修改人',
  PRIMARY KEY (`withdrawal_id`),
	UNIQUE KEY `accp_txno_index` (`accp_txno`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='提现记录';

CREATE TABLE `96365_mall`.`lian_lian_account` (
  `account_id` varchar(20) NOT NULL COMMENT '账户ID',
  `merchant_id` varchar(20) NOT NULL COMMENT '商户ID',
  `account_open_params` text NOT NULL COMMENT '开户参数',
  `update_params_date` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '开户参数更新时间',
  `account_status` varchar(30) NOT NULL DEFAULT '0' COMMENT '连连开户状态',
  `oid_userno` varchar(30) DEFAULT NULL COMMENT '连连用户ID',
  `reg_phone` varchar(15) DEFAULT NULL COMMENT '连连注册绑定手机号',
  `linked_acctno` varchar(32) DEFAULT NULL COMMENT '绑定银行账号',
  `linked_agrtno` varchar(32) DEFAULT NULL COMMENT '绑卡协议号',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注，审核不通过时，该字段内容是失败原因',
  `remittance` varchar(500) DEFAULT NULL COMMENT '待打款验证信息',
  `del_flag` varchar(1) DEFAULT '1' COMMENT '逻辑删除 （1 正常 0删除）',
  `create_date` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `create_id` varchar(20) DEFAULT NULL COMMENT '创建人',
  `modify_date` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  `modify_id` varchar(20) DEFAULT NULL COMMENT '修改人',
  PRIMARY KEY (`account_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='连连账户信息';

CREATE TABLE `lian_lian_bank_code` (
  `id` varchar(20) NOT NULL COMMENT 'ID',
  `bank_name` varchar(50) NOT NULL COMMENT '银行名称',
  `bank_code` varchar(30) NOT NULL COMMENT '银行编码',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='连连银行编码';

ALTER TABLE 96365_order.order_main MODIFY `sub_account_status` VARCHAR(20) DEFAULT 'un_transfer' COMMENT '分账状态（TransferStatusEnum）';

-- ALTER TABLE `96365_mall`.merchant ADD COLUMN reg_phone varchar(15) DEFAULT NULL COMMENT '连连注册绑定手机号' AFTER `oid_userno`;

ALTER TABLE `96365_pay`.`pay_message` MODIFY COLUMN `PAYMENT_TYPE` varchar(5) DEFAULT NULL COMMENT '支付类型 关联PaymentType枚举类';

ALTER TABLE `96365_pay`.`notify_message` MODIFY COLUMN `PAYMENT_TYPE` varchar(5) DEFAULT NULL COMMENT '支付类型 关联PaymentType枚举类';

ALTER TABLE `96365_pay`.`pay_message` ADD COLUMN `third_party_id` varchar(80) DEFAULT NULL COMMENT '第三方系统交易单号';

ALTER TABLE `96365_order`.order_payment ADD COLUMN domain VARCHAR(50) DEFAULT NULL COMMENT '域名';

CREATE TABLE `96365_order`.`transfer_flow` (
  `transfer_id` varchar(20) NOT NULL COMMENT '分账ID',
  `object_id` varchar(20) NOT NULL COMMENT '关联ID',
  `transfer_type` varchar(20) NOT NULL COMMENT '关联类型',
  `transfer_from` varchar(20) NOT NULL COMMENT '转发对象ID',
  `transfer_to` varchar(20) NOT NULL COMMENT '被转发对象ID',
  `total_amount` int(10) NOT NULL COMMENT '总金额',
  `transfer_amount` int(10) NOT NULL COMMENT '分账金额',
  `transfer_status` varchar(20) DEFAULT NULL COMMENT '分账状态',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  `ip_address` varchar(100) DEFAULT NULL COMMENT 'ip地址',
  `domain` varchar(50) DEFAULT NULL COMMENT '域名',
  `del_flag` varchar(1) DEFAULT '1' COMMENT '逻辑删除 （1 正常 0删除）',
  `create_date` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `create_id` varchar(20) DEFAULT NULL COMMENT '创建人',
  `modify_date` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  `modify_id` varchar(20) DEFAULT NULL COMMENT '修改人',
  PRIMARY KEY (`transfer_id`),
  UNIQUE KEY `object_index` (`object_id`,`transfer_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='分账流水记录';

ALTER TABLE 96365_order.order_payment ADD COLUMN pay_object varchar(25) NOT NULL DEFAULT 'order_main' COMMENT '支付对象，关联PayObjectTypeEnum';


```