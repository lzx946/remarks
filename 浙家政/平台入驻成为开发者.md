
```sql

ALTER TABLE developer ADD COLUMN token VARCHAR(64) DEFAULT NULL COMMENT '登录token' AFTER remark;

ALTER TABLE developer ADD UNIQUE `sc_code_index` (`sc_code`);
ALTER TABLE developer ADD UNIQUE `contact_phone_index` (`contact_phone`);

```